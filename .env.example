# Never commit real secrets.
# Required when using Gemini-backed word and emotion analysis.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Optional local API server config
PORT=3001
CORS_ORIGIN=http://localhost:5173
